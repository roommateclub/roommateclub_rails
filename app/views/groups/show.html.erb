<% if @user_group_ship.present? %>
<%= simple_form_for(@user_group_ship, url: user_join_group_path(@group)) do |f| %>
  <%= f.input :message %>
  <%= f.button :submit, "傳送請求" %>
<% end %>
<% end %>
  
