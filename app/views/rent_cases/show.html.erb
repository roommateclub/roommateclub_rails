<h1>月租:<%= @rent_case.price %></h1>
<h1>介紹:</h1>
<p><%= @rent_case.apartment.description %></p>
<h1>照片:</h1>
<div class="row">
  <div class="col-xs-6 col-md-3">
  <% @rent_case.apartment.images.each do |image| %>
    <a href="#" class="thumbnail">
    <%= image_tag(image.file) %>
    </a>
  <% end %>
  </div>
</div>
<h2>別人發起的合租計畫</h2>
<table class="table">
  <thead>
    <tr>
      <th>合租計畫</th>
      <th>人數</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @groups.each do |group| %>
    <tr>
      <td><%= group.name %></td>
      <td><%= group.group_size %></td>
      <td><%= link_to("加入 Group", group_path(group)) %></td>
    </tr>
  <% end %>
  </tbody>
</table>
  