<%= @rent_case.price %>
<table class="table">
  <thead>
    <tr>
      <th>人數</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @groups.each do |group| %>
    <tr>
      <td><%= group.group_size %></td>
      <td><%= link_to("加入 Group", group_path(group)) %></td>
    </tr>
  <% end %>
  </tbody>
</table>
  